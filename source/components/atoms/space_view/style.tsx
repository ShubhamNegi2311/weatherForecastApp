/* REACT */
import {StyleSheet} from 'react-native';

export const style = (height: number, width: number) =>
  StyleSheet.create({
    container: {
      height: height,
      width: width,
    },
  });

export default style;
